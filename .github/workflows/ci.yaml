name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write # Required for creating releases and pushing tags
  id-token: write # Required for npm trusted publishing (OIDC)

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm run typecheck
      - run: npm test

  example-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('examples/react-router/tests/package.json') }}
      - name: Install Playwright browsers
        run: npm run install-browsers --workspace=@activescott/auth-example-react-router-e2e
      - name: Run e2e tests
        run: npm run e2e --workspace=@activescott/auth-example-react-router-e2e
      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: examples/react-router/tests/playwright-report
          retention-days: 7

  release:
    needs: [validate, example-e2e]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    outputs:
      packages: ${{ steps.get-packages.outputs.packages }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run release script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx tsx scripts/release.ts

      - name: Get packages to publish
        id: get-packages
        run: |
          if [ -f packages-to-publish.json ]; then
            packages=$(cat packages-to-publish.json)
            echo "packages=${packages}" >> "$GITHUB_OUTPUT"
          else
            echo "packages=[]" >> "$GITHUB_OUTPUT"
          fi

  publish:
    needs: release
    runs-on: ubuntu-latest
    if: needs.release.outputs.packages != '[]'
    strategy:
      # One package failing to publish must not cancel the others —
      # their tags already exist, and a cancelled publish has no retry
      # path (reruns use the original workflow snapshot and re-cancel).
      fail-fast: false
      matrix:
        package: ${{ fromJson(needs.release.outputs.packages) }}

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          # Node 24 ships with npm 11, which is required for npm's OIDC
          # trusted publishing (>=11.5.1). Node 22's bundled npm 10 would
          # need an `npm install -g npm@latest` self-upgrade, which broke
          # after a regression in Node 22.22.2's toolcache npm
          # (npm/cli#9151) — `Cannot find module 'promise-retry'`.
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org

      - run: npm ci

      - name: Build all packages
        run: npm run build

      # No NODE_AUTH_TOKEN env: setting it (even to empty) makes npm prefer
      # token auth over OIDC, which then 404s. Trusted publishing requires:
      #   - `id-token: write` permission (set at the workflow level above)
      #   - npm >= 11.5.1 (Node 24 ships npm 11)
      #   - the trusted-publisher relationship configured on npmjs.com for
      #     this repo + workflow file (`ci.yaml`)
      # Provenance attestations are generated automatically — no `--provenance`
      # flag needed.
      - name: Publish ${{ matrix.package.name }}
        working-directory: ${{ matrix.package.dir }}
        run: npm publish --access public
